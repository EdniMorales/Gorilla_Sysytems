<IfModule mod_rewrite.c>
    # Activar reescritura de URLs
    RewriteEngine On

    # Redirigir si NO es archivo o directorio existente
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d

    # Si se accede a /controlador/accion
    RewriteRule ^([^/]+)/([^/]+)?$ index.php?controller=$1&action=$2 [QSA,L]

    # Si se accede a la raíz (sin controlador ni acción)
    RewriteRule ^$ index.php [QSA,L]
</IfModule>

# 👇 OPCIONAL: redirección forzada a HTTPS para hosting, desactiva localmente
# RewriteCond %{HTTPS} off
# RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# ❌ OPCIONAL: Esta línea viene de cPanel. Coméntala o elimínala para uso local.
# <IfModule mime_module>
#   AddHandler application/x-httpd-ea-php81___lsphp .php .php8 .phtml
# </IfModule>
